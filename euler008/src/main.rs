//take a number string, find the 4 adjacent numbers with the highest product
fn string_to_nums(s: String) -> [u32; s.len()] {
    let num_strings = s.chars();
    let num_list = num_strings.into_iter().map();
}

fn euler8() -> u32 {}

fn main() {
    let one_thousand_series = 0;
    let test_series = "123123";
    let mut test_pop = test_series.chars();

    println!("Hello, world!");
}
